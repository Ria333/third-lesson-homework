// - створити функцію яка приймає будь-яку кількість чисел, повертає найменьше, а виводить найбільше (Math використовувати заборонено);*/
//
let i=0;
function formula (){
    if ( i<arguments.length, i++) {
        return (arguments[i]); console.log (arguments [i])}
    else if (arguments [i] > arguments.length)
    {return (arguments [i]); document.write (arguments[i])}
};
let a = formula (1,2,3);
console.log(a);

/*function formula (b){

    for (let i=0;i<a.length; i++) {
        if (a[i] < a.length);
        formula=i; {
            return (formula);}}}}}
        return (v arguments[i]); console.log (v [i])}
else if (v [i] > arguments.length)
    {return (v [i]); document.write (v [i])}
} };
let a = formula (1,2,3)
console.log(a)*/

/*не вышло.как прописать в функции любое количество цифр?*/


// - створити функцію яка приймає масив примітивних елементів (числа,стрінги,булеві), та будує для них список
/*function red ([a,'b',c]){
if (a==="boolean"{}*/
//*корректно ли писать function nazva ([])?Или как записать что она приняла массив?что значит список?как записывается список?*/

